language: python
python:
  - 2.7
  - 3.2
  - 3.4

before_install:
  - sudo apt-get install make

install:
  - "pip install -r testrequirements.txt"
  - "pip install -r requirements.txt"

script:
  - make
